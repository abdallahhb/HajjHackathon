<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smart.hajj.hajj">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.smart.hajj.MainActivity"
            android:theme="@style/MainActivity" />
        <activity
            android:name="com.smart.hajj.LoginActivity"
            android:theme="@style/LoginActivity" />
        <activity
            android:name="com.smart.hajj.HajjDataGeneral"
            android:theme="@style/HajjDataDetail" />
        <activity
            android:name="com.smart.hajj.HajjIdInsert"
            android:theme="@style/HajjIdInsert" />
        <activity
            android:name="com.smart.hajj.ReportingAccident"
            android:theme="@style/HajjIdInsert" />
        <activity
            android:name="com.smart.hajj.ReportingLost"
            android:theme="@style/HajjIdInsert" />
        <activity
            android:name="com.smart.settings.Language"
            android:theme="@style/Language" />
        <activity
            android:name="com.smart.hajj.StartSceen"
            android:theme="@style/StartScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.smart.hajj.ReportActivity"
            android:theme="@style/Language" />
        <activity
            android:name="com.smart.hajj.MapsActivity"
            android:theme="@style/ListViewTheme" />
        <activity
            android:name="com.smart.LeslistView.ListOneActivity"
            android:theme="@style/ListViewTheme" />
        <activity
            android:name="com.smart.hajj.SuggestionHajj"
            android:theme="@style/Language" />
        <activity android:name="com.smart.hajj.LoginUsActivity" />
        <activity android:name="com.smart.hajj.NotificationActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.smart.hajj.Activity"
            android:label="@string/title_activity_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.smart.hajj.NewActivity"
            android:theme="@style/NewActivity">


        </activity>
    </application>

</manifest>